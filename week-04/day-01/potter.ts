'use strict';

function totalCost (arr:number[]): number {
  // alapar 8 eur, 2 db 5, 3 db 10, 4 db 20, 5 db 25% kedv
  /* for (let i:number = 0; i < arr.length; i++) {
    if (arr[i] === 0) {
      arr.splice(i, 1);
    }
  } */
  while (arr !== []) {
    arr.forEach()
  }

}

let potter:number[] = [2, 2, 2, 1, 1];

console.log(totalCost(potter));